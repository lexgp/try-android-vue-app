import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.calcunyator3',
  appName: 'calcunyator3',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
